<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Адрес панели редактора
    |--------------------------------------------------------------------------
    |
    | Панель стоит на том же домене, что и сайт, поэтому её адрес сделан
    | неугадываемым и задаётся окружением: сменить — поправить FILAMENT_PANEL_PATH
    | в .env и выполнить `php artisan optimize`. В robots.txt адрес не пишется,
    | иначе он перестал бы быть неугадываемым; от поиска панель закрыта
    | заголовком X-Robots-Tag, см. App\Http\Middleware\NoIndex.
    |
    */

    'path' => env('FILAMENT_PANEL_PATH', 'admin'),

];
